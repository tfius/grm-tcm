[project]
name = "grm-tcm"
version = "0.1.0"
description = "Synthetic GRM-TCM benchmark generator and trainer."
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "numpy>=2.0",
    "pandas>=2.2",
    "scikit-learn>=1.5",
    "scipy>=1.14",
]

[tool.uv]
package = false
